apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: test-data
  name: test-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
